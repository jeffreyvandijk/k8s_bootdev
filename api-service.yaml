apiVersion: v1
kind: Service
metadata:
  name: api-service
spec:
  type: ClusterIP
  selector:
    app: synergychat-api
  ports:
    - protocol: TCP
      nodePort: 30080
      port: 80
      targetPort: 8080
